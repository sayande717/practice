class hello
{
    public static void main(String[] args) 
    /*
     * static = This is so that we can run this function without creating an object of the class.
     * void = Return type
     * String args[] = Takes in command line arguments during runtime.
     * Ex. java hello 30 = This passes 30 to args[].
     * main = the main function. Java runs this function when it executes the program, it is the entry point * of the Java program.
     */
    {
        System.out.print("Hello, World!");
        /*
         * System = Class
         * out = variable
         * println = method of the class System.
         */
    } 
}