#include<stdio.h>
boolean prime(int);
int main()
{
    int n,c;
    printf("Enter a number");
    scanf("%d",&n);
    printf("Press 1 to use function.");
    scanf("%d",&c);
    if(c==1)
    printf("");
}
boolean prime(int n)
{
    int a;
    if(a==2)
    return true;
    else return false;
}